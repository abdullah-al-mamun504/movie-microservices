apiVersion: apps/v1
kind: Deployment
metadata:
  name: movie-service-deployment
  namespace: movie-microservices
spec:
  replicas: 2
  selector:
    matchLabels:
      app: movie-service
  template:
    metadata:
      labels:
        app: movie-service
    spec:
      containers:
      - name: movie-service
        image: REPLACE_WITH_YOUR_REGISTRY/movie-microservices/movie-service:latest
        ports:
        - containerPort: 3001
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: movie-microservices-config
              key: NODE_ENV
        - name: PORT
          value: "3001"
        - name: MOVIES_DB_NAME
          valueFrom:
            secretKeyRef:
              name: movies-db-secret
              key: database
        - name: MOVIES_DB_USER
          valueFrom:
            secretKeyRef:
              name: movies-db-secret
              key: username
        - name: MOVIES_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: movies-db-secret
              key: password
        - name: MOVIES_DB_HOST
          value: "movies-db-service"
        - name: MOVIES_DB_PORT_INTERNAL
          value: "5432"
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: movie-microservices-config
              key: REDIS_URL
        - name: TMDB_API_KEY
          valueFrom:
            secretKeyRef:
              name: tmdb-api-key
              key: key
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: movie-microservices-config
              key: LOG_LEVEL
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 3001
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: movie-service-logs
          mountPath: /app/logs
      volumes:
      - name: movie-service-logs
        persistentVolumeClaim:
          claimName: movie-service-logs-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: movie-service
  namespace: movie-microservices
spec:
  selector:
    app: movie-service
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: movie-service-logs-pvc
  namespace: movie-microservices
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: movie-service-hpa
  namespace: movie-microservices
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: movie-service-deployment
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
